# Срез проекта

> Last reviewed: 2026-09-25

Что сделано, что дальше, что мешает. Обновляется при каждом мерже (`docs/AGENT_RULES.md` §4.3).

Статусы: ✅ проверено на живом портале · 🔌 REST проверен на тестовом портале вебхуком
администратора (2026-09-24: запросы и ответы, сквозной расчёт и запись в счёт чистыми функциями
приложения), но не из фрейма установленного приложения · 🧪 код и тесты есть, вживую не гонялось ·
⏳ не начато.

## Сделано

«Тесты» — юнит-тесты чистой логики (`tests/`); страницы и обращения к порталу тестами не
покрыты — их проверит живой прогон.

| Что | Статус | Где | Тесты |
|---|---|---|---|
| Каркас: Nuxt 4 + b24ui + b24jssdk + Nitro, CI, dependabot, Docker | 🧪 (сборка — в CI) | `nuxt.config.ts`, `.github/` | гарды репо |
| Выкат как в client-bank: `main` → GHCR → Watchtower → nginx-proxy, `invoice-from-tasks.bx-shef.by` | 🧪 (собранный сервер с прод-окружением проверен локально; сервер не поднят) | `docker-compose.prod.yml`, `Makefile`, `.github/workflows/ci.yml` | гарды CI |
| Процесс: правила, панель ревью, мерж, навыки | 🧪 | `docs/AGENT_RULES.md`, `.claude/skills/` | штампы, CI |
| Смок на тестовом портале: страж портала, засев, формы REST, 36 вариантов расчёта, запись, BitrixGPT | ✅ (64/64, 2026-09-25) | `smoke/`, `pnpm smoke` | окружение и страж — юнит-тесты |
| Установка: права, встройка в карточку счёта, события, снятие старых подписок | 🧪 | `app/pages/install.vue` | шаги (`app/utils/install.ts`) |
| Главная: готовность настроек; администратору — что не настроено на сервере и код приложения | 🧪 | `app/pages/app.vue` | разбор `/api/health` (`app/utils/serverHealth.ts`) |
| Заполнение счёта: источники, тип 1/2, округление (вверх/к ближайшему), ставки по датам, наценки по тегам, пересчёт валюты | 🔌 | `shared/domain/fill.ts`, `app/pages/invoice.vue` | расчёт, листание страниц, итог записи и разбор пакета — да; сценарий страницы целиком — нет |
| Остановка при нехватке данных со списком всех проблем | 🔌 | `shared/domain/fill.ts` | да |
| Названия строк через BitrixGPT, редактируемые промпты | 🔌 (модель — ключом Вайбкода в смоке; эндпоинт `/api/ai/names` — нет) | `server/api/ai/names.post.ts` | промпты, разбор ответа, лимиты |
| Консультации BitrixGPT → дело в счёте | 🔌 (модель и дело; эндпоинт `/api/ai/consult` — нет) | `server/api/ai/consult.post.ts` | параметры дела, обезвреживание разметки |
| Настройки: общие, ставки, наценки, промпты, доступ, хранилище | 🧪 | `app/pages/settings.vue` | формат и разбор |
| Ставки редактируют не-администраторы (через сервер токеном установщика) | 🧪 | `server/utils/optionWrites.ts` | да: кто и каким токеном пишет |
| Замер предела `app.option` с копией и восстановлением | 🧪 (проверен на имитации) | `app/utils/storageProbe.ts` | да |
| Сервер: события установки со сверкой `member_id` и домена на сервере авторизации портала, шифрование токенов, SSRF-гард, CSP, пределы запросов, лимиты (BitrixGPT — в строках) | 🧪 (события и пределы — ещё и запросами к собранному серверу) | `server/` | да |

## Ворота перед боевым порталом

Пока не пройден живой прогон установленного приложения, оно **не ставится на рабочий портал** с
настоящими счетами: запись в счёт необратима (`docs/AGENT_RULES.md` §5.8). REST расчёта и записи
уже проверен на тестовом портале вебхуком (🔌, `docs/REST_METHODS.md` — ✅); не проверено то,
что видно только из установленного приложения: установка, права сотрудника во фрейме,
`app.option`, встройка, события, BitrixGPT. Прогон — по `docs/DEPLOY.md` §3.

## Дальше

1. **Установка на тестовый портал** ([#2](https://github.com/bx-shef/invoice-from-tasks/issues/2)).
   Сначала — владельцу, порядок `docs/DEPLOY.md` §1–2: DNS `invoice-from-tasks.bx-shef.by` → сервер;
   пакет GHCR публичный; `.env` и `make prod-up` на сервере; приложение в кабинете разработчика
   Маркета, ключи и код — в `.env`; «Протестировать» → «Установить» на тестовом портале.
   REST расчёта и записи уже проверен вебхуком и повторяется смоком (`pnpm smoke`, 2026-09-25):
   задачи и фильтры, теги (из v2-списка задач), записи времени и их листание, курсы, запись позиций (заменить и добавить, пакет с ошибкой),
   дело консультации, имена сотрудников — всё сошлось с кодом; найдено одно расхождение — пустые
   названия единиц измерения (исправлено). Остаётся то, что видно только из приложения,
   порядок — `docs/DEPLOY.md` §3:
   - предел `app.option` (кнопка «Замерить предел»);
   - какой код привязки (`SI_<id>` или `T1f_<id>`) ставит сам интерфейс, привязывая задачу из
     карточки счёта (REST находит оба);
   - правами обычного сотрудника во фрейме: **запись позиций счёта** (`crm.item.productrow.set` /
     `.add` — самое чувствительное место: пишет сотрудник своими правами), `crm.currency.list`,
     `tasks.task.result.list` v3 (вебхук был администраторский);
   - установка доходит до сервера: событие приходит с `Content-Length` (иначе `411`), в нём есть
     `auth[server_endpoint]` и он из белого списка, сервер авторизации отвечает на продление,
     `client_endpoint` — адрес портала; если есть портал другой зоны (`.com`, `.de`) — какой
     сервер авторизации он называет;
   - главная показывает администратору код приложения, пока `B24_APP_CODE` не задан;
   - `/api/health` через свой прокси показывает `request.forwardedFor: used`;
   - дело консультации: `DESCRIPTION_TYPE = 3` рисует разметку, переносы строк на месте;
   - «добавить к товарам» при отказе посередине — счёт перечитывается, «добавлено N из M»;
     при медленном портале строки не задваиваются (повторы SDK на записи выключены).
2. НДС в строках ([#4](https://github.com/bx-shef/invoice-from-tasks/issues/4)). Правила
   расчёта владелец утвердил в [#3](https://github.com/bx-shef/invoice-from-tasks/issues/3):
   округление настройкой, дата записи — дата создания, наценка по тегам вместо товаров, пересчёт
   валюты по курсу портала (`docs/PROCESSING.md`, «Решения владельца»).
3. Сервер авторизации `oauth.bitrix.info` — нужен ли в белом списке ([#8](https://github.com/bx-shef/invoice-from-tasks/issues/8)); выяснится на
   живом прогоне.
4. Генерация документа по задачам — владелец опишет отдельно ([#10](https://github.com/bx-shef/invoice-from-tasks/issues/10)).

## Потом

- Визуальные регресс- и e2e-тесты страниц (Playwright), как в эталоне ([#9](https://github.com/bx-shef/invoice-from-tasks/issues/9)).
- Перевести список задач с тегами на REST v3, когда v3-список научится фильтру по привязке к CRM
  и тегам ([#13](https://github.com/bx-shef/invoice-from-tasks/issues/13)); записи времени — отдельно,
  когда в v3 появится их метод. Проверять руками время от времени.
- Ротация ключа шифрования токенов `B24_TOKEN_ENC_KEY` ([#5](https://github.com/bx-shef/invoice-from-tasks/issues/5)).
- Общий счётчик лимитов, очередь токена установщика и хранилище установок, если серверов станет
  больше одного: сейчас всё в памяти одного процесса и на его томе ([#6](https://github.com/bx-shef/invoice-from-tasks/issues/6)).
- Атомарное снятие индекса домена ([#7](https://github.com/bx-shef/invoice-from-tasks/issues/7)).

## Что мешает

- **Приложение ещё не установлено на тестовый портал.** Вебхук владельца (2026-09-24) дал
  проверить REST, но не установку, фрейм и права обычного сотрудника. Выкат готов (адрес
  `invoice-from-tasks.bx-shef.by`, схема client-bank); осталось поднять сервер и
  зарегистрировать приложение в Маркете — это делает владелец (`docs/DEPLOY.md` §1–2), поэтому
  пункт 1 «Дальше» первый.
- **Предел `app.option` не известен.** По документации ядра — 2000 символов; пока не замерен,
  приложение считает по нему, и длинные свои промпты не сохранятся.
