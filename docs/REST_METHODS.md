# Исходящие вызовы REST Битрикс24

> Last reviewed: 2026-09-26

Реестр каждого метода, который зовёт приложение. Добавили вызов в код — добавьте строку сюда:
`tests/repoGuards.test.ts` сверяет строковые имена методов в `app/`, `server/`, `shared/` с этим
файлом и краснеет на пропуске. Из этого же списка собирается набор прав приложения.

Сигнатуры здесь не дублируются: их источник — MCP `b24-dev-mcp` (`bitrix-search` →
`bitrix-method-details`). Здесь — то, чего в документации нет: кто зовёт, каким транспортом, чьими
правами и на какие грабли уже наступили.

**Транспорт.** «Фрейм» — `@bitrix24/b24jssdk` во фрейме портала (`app/composables/useB24.ts`),
правами сотрудника, открывшего страницу. «Сервер: фрейм-токен» — наш Nitro зовёт портал токеном
того же сотрудника (`server/utils/b24Client.ts → makeFrameCall`). «Сервер: установщик» — токеном
администратора, установившего приложение (`makePortalCall`), только там, где прав сотрудника не
хватает по документации.

**Статус.** 📄 — сверено с документацией MCP, на живом портале НЕ проверялось; ✅ — проверено
на тестовом портале (2026-09-24) **вебхуком администратора** тем же b24jssdk и теми же
параметрами, что в коде: форма запроса и ответа, сквозной сценарий счёта на чистых функциях
приложения. Из фрейма и правами обычного сотрудника это ещё не прогонялось, а методы, которым
нужен контекст приложения (`app.option.*`, `placement.*`, `event.*` — вебхуку «Application context
required»; `app.info` отвечает без `CODE`), остаются 📄 до установки на портал
([#2](https://github.com/bx-shef/invoice-from-tasks/issues/2)).

**Версия REST.** Правило владельца: работаем через b24jssdk и **берём REST v3, где метод в нём
есть**. По документации (статья «Обзор REST 3.0», сверка 2026-09-24) v3 сейчас покрывает только
задачи (и модули, которых мы не зовём: база знаний, почта, структура компании, журнал событий,
учёт рабочего времени). Поэтому:

- **v3** (`b24.callV3` → `actions.v3.call`, адрес `/rest/api/…`): `tasks.task.result.list`.
  Фильтр — массив троек `[поле, оператор, значение]`, связанные объекты — через точку в `select`,
  логические значения — `true/false`, не `Y/N`.
- **v2** — всё остальное: CRM, каталог, сотрудники, настройки, встройки, события — в v3 их нет;
  `tasks.task.list` — фильтр v3 понимает только `id`, а нам нужен поиск по привязке к CRM
  (замер: `crmItemIds` — «требуется атрибут Filterable»); `task.elapseditem.getlist` — метода v3
  нет, `elapsedTime` в v3 `tasks.task.get` не возвращается (замер).
- **Исключение владельца — теги задач из v2** (2026-09-24): v3 отдаёт теги только в
  `tasks.task.get` — отдельный запрос на задачу (пакетами по 50), а пакет v3 при одной ошибке
  отказывает целиком. v2 `tasks.task.list` с `select: ['TAGS']` отдаёт их в том же списке, что
  мы и так читаем, — ноль лишних запросов. Вернуться к v3 — issue
  [#13](https://github.com/bx-shef/invoice-from-tasks/issues/13).
- Пакет v3 — отдельный запрос: смешивать в нём методы v2 нельзя (у версий разные адреса), и
  портал выполняет его целиком или никак (замер).
- Ограничения v3 выше — замер 2026-09-24. Смок их не проверяет: владелец решил перепроверять
  руками время от времени (issue #13). Появится нужное в v3 — переводим и отмечаем здесь. Формы
  ответов методов, которые вызывает приложение, держит смок (`pnpm smoke`, `docs/SMOKE.md`).

## Счёт и товарные позиции (право `crm`)

| Метод | Транспорт | Файл | Назначение, грабли | Статус |
|---|---|---|---|---|
| `crm.item.get` | фрейм | `useInvoiceFill.ts` | Счёт: `entityTypeId = 31`. Сделка — в `parentId2` (статья MCP «Счета: обзор методов и событий»: «Передавайте ID сделки в параметре `parentId2`»), валюта — `currencyId`, «Реквизиты вашей компании» — `mycompanyId` (по ним ставка НДС, `shared/domain/vat.ts`). Замер: ответ `{ item: { id, parentId2, currencyId, mycompanyId, opportunity, taxValue, … } }` — как разбирает `parseInvoice`. ⚠ Замер 2026-09-26: реквизиты не выбраны — `mycompanyId: 0`; новый счёт портал **сам** получает «моей компанией» по умолчанию, даже с `mycompanyId: 0` в `crm.item.add` — снять можно только `crm.item.update`. | ✅ |
| `crm.item.list` | фрейм | `useMyCompanies.ts` (вкладка «НДС») | «Реквизиты вашей компании»: `entityTypeId = 4`, фильтр `isMyCompany: 'Y'` (статья «Счета: обзор методов и событий»), `select: ['id', 'title']`, листание `start` по 50. Параметры — `myCompaniesCall` (`app/utils/invoiceRequests.ts`). Замер 2026-09-26: ответ `{ items: [{ id, title }] }`, `total` рядом; компании без признака в список не попадают (смок). У поля счёта `mycompanyId` в `crm.item.fields` — тип `crm_company`, `settings.isMyCompany: true`, название «Реквизиты вашей компании». | ✅ |
| `crm.item.productrow.list` | фрейм | `useInvoiceFill.ts`, листание — `app/utils/paging.ts` | Существующие позиции: фильтр `=ownerType: 'SI'`, `=ownerId`. Замер: страница — 50 позиций, `start` 0/50/100, за концом — пусто; `collectOffsetPages` на 55 позициях — 2 запроса. | ✅ |
| `crm.item.productrow.set` | фрейм | `useInvoiceFill.ts` | Режим «заменить»: ЗАМЕНЯЕТ все позиции переданным набором. Замер: позиция без товара каталога сохраняется с `productId: 0`, `measureCode` и `measureName` («шт» для 796), `taxRate: null`; сумму счёта (`opportunity`) портал пересчитывает сам — совпала с итогом предпросмотра. В ответе есть `priceAccount` — цена в базовой валюте портала. **НДС** (замер 2026-09-26, счёт из 9 строк): `price` — цена **с налогом** (документация: «с учетом скидок и налогов»), `priceExclusive` / `priceNetto` — только чтение, портал выделяет их сам: `price / (1 + taxRate/100)`, до 8 знаков. `taxIncluded` на это не влияет (Y и N при цене 204 и 20% дают 170) — он только про показ. Цену с дробью (120,036) портал хранит как есть — поэтому шлём её без округления до копеек (`grossPrice`). `taxName` («НДС 20%») портал берёт из `catalog.vat.list` сам. `taxRate: null` — «Без НДС», `0` — «НДС 0%». Итог счёта: `opportunity` — Σ цена × количество, одно округление; `taxValue` — налог по строкам, каждая округлена — как считает `vatTotals`, сошлось до копейки. | ✅ |
| `crm.item.productrow.add` | фрейм, batch | `useInvoiceFill.ts` | Режим «добавить»: по одной позиции, остальные не трогает. Налоговые поля — как у `set` (замер 2026-09-26). ⚠ `set` для этого не годится: у позиции без переданной цены цена станет 0 (документация `set`). Пакет — с остановкой на первой ошибке; ⚠ автоматические повторы SDK при сетевых сбоях выключены для всего фрейма (иначе запрос, выполненный порталом, но с опоздавшим ответом, ушёл бы ещё раз — дубли). После записи счёт перечитывается: «добавлено N из M», лишние строки — предупреждение, «ответ — ошибка, но все строки в счёте» — предупреждение «повторять не нужно» (`app/utils/writeOutcome.ts`). Ошибка команды в пакете называет её метод. Замер пакета с `halt` и ошибкой второй команды из трёх: ответов два (успех и ошибка «Owner was not found»), третья не выполнена — как ждёт `unwrapBatchPart`; первая строка осталась в счёте. | ✅ |
| `crm.currency.list` | фрейм | `SettingsGeneral.vue`, `useInvoiceFill.ts`, разбор — `shared/domain/currency.ts` | Список валют для «валюты ставок» (`CURRENCY`, `FULL_NAME`) и курсы для пересчёта в валюту счёта: курс — цена `AMOUNT_CNT` единиц в базовой валюте (`AMOUNT`), базовая — `BASE = 'Y'`, дата — `DATE_UPDATE`; числа строками. Постраничности нет (`total` всегда 0 — документация). Курса валюты нет — заполнение останавливается. Замер: ответ — как в документации (`BASE = Y` у базовой, `DATE_UPDATE` ISO). ⚠ Правами обычного сотрудника не проверено. | ✅ |
| `crm.activity.todo.add` | фрейм | `useInvoiceFill.ts`, параметры — `shared/domain/activity.ts` | Ответ консультации делом в счёте (`ownerTypeId = 31`). Схема — как в ai-price-import (там проверено вживую). Замер: ответ `{ id }`; скобки разметки в заголовке и описании приходят обезвреженными (полноширинными). | ✅ |
| `crm.activity.update` | фрейм | `useInvoiceFill.ts` | Сразу после `todo.add`: `DESCRIPTION_TYPE = 3`, иначе переносы строк ответа слипаются. У `todo.add` такого параметра нет. Метод помечен устаревшим, но другого пути нет — так делает и ai-price-import. ⚠ Документация и замер расходятся: по `crm.enum.contenttype` 3 — это HTML, а замер ai-price-import (06.08.2026) показал, что при 3 BB-разметка отрисовывается, а при 2 — видна исходником. Следуем замеру и обезвреживаем в тексте и BB-, и HTML-скобки (`shared/domain/activity.ts`). Замер: `DESCRIPTION_TYPE` после вызова — `"3"`. Как карточка отрисует разметку и переносы — смотреть глазами в интерфейсе. | ✅ |

## Задачи (право `task`)

| Метод | Транспорт | Файл | Назначение, грабли | Статус |
|---|---|---|---|---|
| `tasks.task.list` | фрейм, `callList` | `useInvoiceFill.ts` | Задачи по привязке `UF_CRM_TASK`. ⚠ Фильтр — ОБЪЕКТОМ (`{ UF_CRM_TASK: 'D_5' }`): MCP показывает форму REST v3 (массив), а `/rest/` её отвергает с 400 — замер get-task-from-b24. ⚠ Непонятый фильтр портал НЕ отвергает, а отдаёт все задачи: привязку перепроверяем сами (`shared/domain/tasks.ts → tasksBoundTo`). Ответ — `{ tasks: [...] }`, ключи camelCase, числа строками. `callList`: `idKey: 'id'`, `cursorIdKey: 'ID'`. Замер подтвердил всё выше: фильтр массивом → 400 «Invalid filter value», непонятный ключ фильтра → все задачи, поиск по коду без учёта регистра (`T1F_2` находит `T1f_2`); `timeSpentInLogs` при нуле — `null`. Теги — в этом же списке: `select` с `TAGS` (`TASK_SELECT`, `app/utils/invoiceRequests.ts`) → `tags: { "<id>": { id, title } }`, пусто — `[]`; разбор — `parseTaskTags` (исключение из «v3, где есть» — см. «Версия REST», #13). ⚠ Тег — одна запись на весь портал: задача с «Дизайн» получит уже существующий «дизайн» — теги сравниваются без учёта регистра. | ✅ |
| `task.elapseditem.getlist` | фрейм | `useInvoiceFill.ts`, листание — `app/utils/paging.ts` | Записи затраченного времени задачи. ⚠ Параметры ПОЗИЦИОННЫЕ, массивом: `[taskId, order, filter, select, params]` — как в примере документации. Страница — не больше 50 (`NAV_PARAMS.nPageSize`), листаем по `total` и числу СЫРЫХ строк; записи дедуплицируются по `ID` на случай, если портал за концом отдаст последнюю страницу повторно. Ключи UPPER_CASE: `USER_ID`, `SECONDS`, `COMMENT_TEXT`, `CREATED_DATE` (дата записи — всегда дата создания, решение по #3; `DATE_START` не читаем). Аналога в v3 нет. Замер: `total` в ответе нет, за последней страницей — пусто (повтора страницы нет, дедупликация не мешает); `collectNumberedPages` на 55 записях — 2 запроса. Даты — со смещением портала (`+03:00`). `CREATED_DATE` через REST можно задать задним числом (`task.elapseditem.add`, права администратора). | ✅ |
| `tasks.task.result.list` | фрейм, **v3** | `useInvoiceFill.ts` | Отчёты задачи — контекст названий в режиме BitrixGPT (тип 1). Метод REST v3: условие по `taskId` в `filter` обязательно, `pagination.limit`, ответ — `text` у каждого результата. Шлём тройку `['taskId', '=', id]` (форма фильтра из «Обзора REST 3.0», её знают типы SDK); пример страницы метода — пара `['taskId', 51]`. Какую форму примет портал — проверить на смоке #2. Сбой не останавливает заполнение. Замер: принимает и тройку, и пару; ответ — `{ items: [{ id, text }] }`; без фильтра — ошибка валидации. Через `/rest/` (v2) метод тоже отвечает — массивом. | ✅ |

⚠ **Право называется `task`**, хотя страницы `tasks.task.*` пишут `tasks`: документация
противоречит сама себе, живой портал принял `task` (замер get-task-from-b24, 2026-08-26).

**Код привязки задачи к счёту.** Для сделки — `D_<id>` (документация). Для нового счёта —
`SI_<id>`: так его называет v3 (`crmItemIds`: «`SI_XX` — счёт»), а в настройках поля
`UF_CRM_TASK` на тестовом портале тип значится `SMART_INVOICE` (замер 2026-09-24). Портал хранит
код как записали: задачи с `SI_2` и с `T1f_2` (31 = 0x1f) обе находятся своим фильтром, — поэтому
ищем по обоим (`crmBindingCodes`). Какой код ставит сам интерфейс, привязывая задачу из карточки
счёта, — посмотреть глазами на #2.

## Каталог (право `catalog`)

Товары и папки каталога приложение не использует с #3 (наценка — по тегам задач, строки —
свободные позиции). Право `catalog` — ради единиц измерения и ставок НДС.

| Метод | Транспорт | Файл | Назначение, грабли | Статус |
|---|---|---|---|---|
| `catalog.vat.list` | фрейм | `useCatalog.ts` (вкладка «НДС») | Ставки НДС портала — варианты выбора для «Реквизитов вашей компании». Параметры — `vatListCall`. Замер 2026-09-26: `{ vats: [{ id, name, rate, active, sort }] }`, `rate` — число; на тестовом портале одна ставка — «НДС 20%». Неактивные отбрасывает `parseVatRates`; «Без НДС» вкладка добавляет сама. Сбой не мешает: остаются «Без НДС» и уже сохранённые ставки. | ✅ |
| `catalog.measure.list` | фрейм | `useCatalog.ts` | Единицы измерения для строк: `code`, `measureTitle`. ⚠ Замер: у системных единиц `measureTitle` и `symbol` — `null`, есть только `symbolIntl` / `symbolLetterIntl`. Название берём из ОКЕИ по коду (`app/utils/measures.ts`). На тестовом портале часа (356) в справочнике нет. | ✅ |

## Сотрудники (право `user_brief`)

| Метод | Транспорт | Файл | Назначение, грабли | Статус |
|---|---|---|---|---|
| `user.get` | фрейм, batch | `useUsers.ts` | Имена сотрудников для ставок, ошибок и предпросмотра. Сбой не критичен — показываем `#ID`. Замер: `{ ID }` в пакете — массив из одного сотрудника; несуществующий ID — пустой массив, не ошибка. | ✅ |

## Настройки приложения (базовое право)

| Метод | Транспорт | Файл | Назначение, грабли | Статус |
|---|---|---|---|---|
| `app.option.get` | фрейм; сервер: фрейм-токен | `useAppSettings.ts`, `server/utils/options.ts` | Чтение настроек и ставок. Доступно любому сотруднику. Без `option` — все опции (нужно для подсчёта места). | 📄 |
| `app.option.set` | сервер: фрейм-токен (админ) / установщик (редактор ставок) | `server/api/settings.post.ts`, `server/api/rates.post.ts` | ⚠ **Только администратор** («Administrator authorization required» — документация). Поэтому не-администратора из списка редакторов ставок сервер пишет токеном установщика. Размер не документирован — см. `docs/SETTINGS.md`. | 📄 |
| `app.info` | сервер: фрейм-токен; фрейм | `server/utils/frameAuth.ts`, `pages/app.vue` | `CODE` — принадлежит ли фрейм-токен нашему приложению. Сверяется с `B24_APP_CODE` всегда: без переменной сервер отвечает 503, а не пропускает проверку. Пока переменная не задана, главная показывает `CODE` администратору (страница установки не годится: после `installFinish` портал её перезагружает). ⚠ Вебхуку отдаёт только `SCOPE` и `LICENSE`, без `CODE` (замер 2026-09-24) — сверку `CODE` проверит только установленное приложение. | 📄 |
| `profile` | фрейм; сервер: фрейм-токен | `useAppSettings.ts`, `server/utils/frameAuth.ts` | Кто пришёл: `ID`, `ADMIN`. Этим флагом сервер решает, пускать ли к записи настроек. Замер: `ADMIN` — логическое `true` (не строка), `ID` — строка; строгая проверка `ADMIN === true` в `frameAuth.ts` и `useAppSettings.ts` этому соответствует. Фрейм-токеном не проверялось. | ✅ |
| `scope` | фрейм | `pages/install.vue` | Какие права выданы — установка показывает недостающие. Замер: массив строк прав; у вебхука администратора есть и `task`, и `tasks`. Какие права выдаст установка приложения — проверит `/install`. | ✅ |
| `batch` | фрейм | `useB24.ts` | Пакет до 50 команд (`actions.v2.batch.make`, документация `rest.batch`). Для записи — `isHaltOnError: true` (параметр `halt`): после первой ошибки портал не выполняет остальное, а ответов приходит меньше, чем команд, — `batch()` это ловит и бросает. Пакет v3 приложение не использует (теги — из списка v2); его поведение — целиком или никак — замерено 2026-09-24. Замер: v2 с `halt` — ответов меньше, чем команд, упавшая команда в ответе есть; v3 — при сбое одной команды весь пакет неуспешен и ответов команд нет. | ✅ |

## Встройки и события (право `placement`, базовое)

| Метод | Транспорт | Файл | Назначение, грабли | Статус |
|---|---|---|---|---|
| `placement.get` | фрейм | `pages/install.vue` | Что уже зарегистрировано: переустановка не должна дать второй пункт меню (точка допускает несколько регистраций). | 📄 |
| `placement.bind` | фрейм | `pages/install.vue`, параметры — `app/utils/install.ts` | `CRM_SMART_INVOICE_DETAIL_TOOLBAR` → `/invoice`. Только из контекста приложения (вебхуком нельзя). `OPTIONS` точка не поддерживает. Пункт не виден до `installFinish`. | 📄 |
| `placement.unbind` | фрейм | `pages/install.vue` | Снять встройку со старым адресом (переезд сервера). | 📄 |
| `event.get` | фрейм | `pages/install.vue` | Уже оформленные подписки — чтобы не дублировать. | 📄 |
| `event.bind` | фрейм | `pages/install.vue` | `ONAPPINSTALL` / `ONAPPUNINSTALL` → `/api/b24/events`. ⚠ До `installFinish`, иначе установка не придёт. | 📄 |
| `event.unbind` | фрейм | `pages/install.vue`, выбор — `app/utils/install.ts` (`staleEventHandlers`) | Снять подписки на установку и удаление со старым адресом (переезд сервера): `event`, `handler` (документация метода). Сбой не останавливает установку. | 📄 |

## Не REST, но рядом

- `crm.enum.contenttype` — справочник типов описания дела; не вызываем, на него ссылается
  комментарий о расхождении документации и замера (`crm.activity.update` выше).
- `$b24.installFinish()`, `$b24.parent.setTitle()`, `$b24.dialog.selectUser(s)()`,
  `$b24.placement.options` — методы фрейма b24jssdk (документация: `https://bitrix24.github.io/b24jssdk/llms.txt`).
- `https://<сервер авторизации>/oauth/token/` — продление токена: при установке — для сверки
  `member_id` и домена (`server/utils/verifyInstallMember.ts`), для токена установщика — внутри
  b24jssdk (`server/utils/b24Client.ts`). Сервер — из `auth[server_endpoint]` события установки
  (документация события: «Адрес сервера авторизации для обновления токена»), только из белого
  списка `oauth.bitrix24.tech` / `oauth.bitrix.info` (`b24Host.ts → resolveOAuthHost`), и
  хранится в записи о портале. Без поля — `oauth.bitrix24.tech` (текущая документация: «Автоматическое
  продление токенов OAuth 2.0», «Коды ошибок сервера авторизации»). 📄
- В ответе продления `client_endpoint` — REST-адрес **портала**, а `domain` — хост **сервера
  авторизации**. Домен установки сверяем по `client_endpoint`.
- `keepAuthFresh` из документации b24jssdk в установленной 2.2.0 нет — токен фрейма перед
  запросом к нашему серверу обновляем сами (`app/utils/frameToken.ts`, `docs/ARCHITECTURE.md`).
- b24jssdk по умолчанию **повторяет** запрос при сетевой ошибке, таймауте и ответе 5xx
  (`retryOnNetworkError`, до 3 попыток — код пакета 2.2.0). Приложение пишет то, что повторять
  нельзя (`crm.item.productrow.add`, `crm.activity.todo.add`, `placement.bind`), поэтому повторы
  выключены один раз при создании фрейма (`app/config/b24.ts → sdkRestrictionParams`,
  `initializeB24Frame({ restrictionParams })`). Невыполненные порталом запросы (лимиты, 429) SDK
  по-прежнему повторяет. Чтение при сетевом сбое само не повторяется — сотрудник повторит кнопкой.
- `https://vibecode.bitrix24.tech/v1` — BitrixGPT (OpenAI-совместимый AI Router Вайбкода), `server/utils/llm.ts`.
